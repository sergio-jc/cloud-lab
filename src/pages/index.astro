---
import Welcome from "../components/Welcome.astro";
import Layout from "../layouts/Layout.astro";

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout>
  <header>
    <div class="logo">🍋 LemonCode - Cloud Lab</div>
    <nav>
      <a href="#cursos">Cursos</a>
      <a href="#comunidad">Comunidad</a>
      <a href="#contacto">Contacto</a>
    </nav>
  </header>

  <main>
    <section class="hero">
      <h1>Domina el código con estilo 🍋</h1>
      <p>
        Aprende desarrollo web, aplicaciones y más, desde una plataforma
        interactiva en modo oscuro con tonos verde limón.
      </p>
      <a href="#cta" class="btn-primary">Comenzar Gratis</a>
    </section>

    <section id="cursos">
      <h2>Nuestros Cursos</h2>
      <div class="cards">
        <div class="card">
          <h3>HTML & CSS</h3>
          <p>Domina la estructura y el diseño de páginas web desde cero.</p>
        </div>
        <div class="card">
          <h3>JavaScript Moderno</h3>
          <p>Aprende a crear aplicaciones dinámicas y funcionales para web.</p>
        </div>
        <div class="card">
          <h3>React + Astro</h3>
          <p>Crea sitios ultra rápidos y dinámicos con React y Astro.</p>
        </div>
      </div>
    </section>

    <section id="comunidad">
      <h2>Comunidad Activa</h2>
      <p>
        Únete a cientos de estudiantes compartiendo proyectos, desafíos y buenas
        prácticas.
      </p>
    </section>

    <section id="cta" class="cta">
      <h2>¡Empieza ahora!</h2>
      <a href="#" class="btn-primary">Únete a LemonCode</a>
    </section>
  </main>

  <footer>
    <p>&copy; 2025 LemonCode. Todos los derechos reservados.</p>
  </footer>
</Layout>
<style>
  :root {
    --color-bg: #0e0e0e;
    --color-primary: #d5f75e;
    --color-primary-dark: #b6db48;
    --color-text: #e0e0e0;
    --color-card: #1a1a1a;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    background-color: var(--color-bg);
    color: var(--color-text);
    font-family: "Segoe UI", sans-serif;
    line-height: 1.6;
  }

  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    background-color: var(--color-card);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
  }

  .logo {
    font-size: 1.8rem;
    font-weight: bold;
    color: var(--color-primary);
  }

  nav a {
    margin-left: 1.5rem;
    color: var(--color-text);
    text-decoration: none;
    font-weight: 500;
  }

  nav a:hover {
    color: var(--color-primary);
  }

  .hero {
    text-align: center;
    padding: 5rem 2rem 3rem;
  }

  .hero h1 {
    font-size: 3rem;
    color: var(--color-primary);
    margin-bottom: 1rem;
  }

  .hero p {
    font-size: 1.2rem;
    max-width: 600px;
    margin: 0 auto 2rem;
  }

  .btn-primary {
    background-color: var(--color-primary);
    color: #111;
    padding: 0.8rem 1.6rem;
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    display: inline-block;
  }

  .btn-primary:hover {
    background-color: var(--color-primary-dark);
  }

  section {
    padding: 4rem 2rem;
    max-width: 1000px;
    margin: 0 auto;
    text-align: center;
  }

  h2 {
    font-size: 2rem;
    color: var(--color-primary);
    margin-bottom: 2rem;
  }

  .cards {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  }

  .card {
    background-color: var(--color-card);
    padding: 1.5rem;
    border-radius: 0.75rem;
    text-align: center;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.4);
  }

  .card h3 {
    margin-bottom: 1rem;
    color: var(--color-primary);
    font-size: 1.4rem;
  }

  .cta {
    padding: 5rem 2rem;
  }

  footer {
    background-color: var(--color-card);
    text-align: center;
    padding: 2rem;
    font-size: 0.9rem;
    color: #999;
    margin-top: 3rem;
  }
</style>
